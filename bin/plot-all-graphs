#!/bin/sh

for i in data/*/graph-*; do
    echo slow: ./bin/plot-chord "$i" "plot/$(basename $(dirname $i))-$(basename "$i").png"
    ./bin/plot-chord "$i" "plot/$(basename $(dirname $i))-$(basename "$i").png"
done
